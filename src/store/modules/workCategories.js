import { getWorkCategories } from '@/api'
import { WORK_CATEGORIES_FETCH } from '@/store/mutation-types'

const state = {
  repository: []
}

const mutations = {}

const actions = {}

const getters = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}
