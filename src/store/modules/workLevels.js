import { getWorkLevels } from '@/api'
import { WORK_LEVELS_FETCH } from '@/store/mutation-types'

const state = {
  repository: []
}

const mutations = {}

const actions = {}

const getters = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}
