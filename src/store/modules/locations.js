import { getLocations } from '@/api'
import { LOCATIONS_FETCH } from '@/store/mutation-types'

const state = {
  repository: []
}

const mutations = {}

const actions = {}

const getters = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}
